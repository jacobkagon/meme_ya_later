<%= form_for [@receiver, @message] do |f| %>

    <%= f.hidden_field :receiver_id, value: @receiver.id %>

    <%= f.label "Enter your message below" %><br>
    <%= f.text_area :body %>

    <%= f.submit "Send" %>
<% end %>

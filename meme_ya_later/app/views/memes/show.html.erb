
<div class="p-3"></div>
 <img src="<%= @meme.url%>" alt="" class="rounded mx-auto d-block" style="width:50%">
</div>
<div class="container text-center">
<h4>Posted by: <div><strong><%= link_to @meme.user.username, user_path(@meme.user.id) %></strong></div>
on <br><%= @meme.created_at.strftime("%b %d, %Y") %><br></h4>
</div>

<%button_to "delete", meme_path(@meme), method: 'delete'%>

